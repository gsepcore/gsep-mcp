[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'GSEP_HTTP_PORT=${PORT:-3100} node dist/index.js --http'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
